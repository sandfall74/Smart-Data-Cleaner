[project]
name = "backend-dataclean"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "sandfall74", email = "adrianpiedrasan@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
backend-dataclean = "backend_dataclean:main"

[build-system]
requires = ["uv_build>=0.12.12,<0.13.0"]
build-backend = "uv_build"
